import pygame
print(pygame._version_)

class Floor:
    def __init__(self:number):
        #variables of creating floor
        self.number = int
        self.width_floor = 0    
        self.heigh_floor = 0
        self.floors_floor = 0
        self.floors_ceiling = self.floors +self.width  
        self.path_img
        
        def build_floor(self,num_floor):
            self.floors_floor = num_floor * self.heigh_floor 
            self.floors_ceiling = self.floors_floor + self.heigh_floor
            self.left_side = left_screen + timer.width
            self.right_side = self.left_side + self.width_floor
            picture = self.path_img    
